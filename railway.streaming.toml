[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.streaming"

[deploy]
healthcheckPath = "/api/v1/streaming/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
